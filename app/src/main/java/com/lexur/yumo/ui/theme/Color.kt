package com.lexur.yumo.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

val MainColor = Color(0xFFFFD8E5)  // Soft pink main color

// Primary Colors
val Primary = Color(0xFFEA89B1)          // Slightly desaturated pink for primary actions
val PrimaryVariant = Color(0xFFD4B0C0)   // Darker variant for pressed states
val OnPrimary = Color(0xFF3D3D3D)        // Dark text on primary backgrounds

// Secondary Colors
val Secondary = Color(0xFFF5E8ED)        // Very light pink for secondary elements
val SecondaryVariant = Color(0xFFEDE0E5) // Slightly darker secondary
val OnSecondary = Color(0xFF2D2D2D)      // Dark text on secondary backgrounds

// Background Colors
val Background = Color(0xFFFFF9FB)       // Very light pinkish white
val OnBackground = Color(0xFF1C1C1C)     // Dark text on background

// Surface Colors
val Surface = Color(0xFFFFFAFC)          // Card and surface backgrounds
val SurfaceVariant = Color(0xFFF8F0F3)   // Alternative surface color
val OnSurface = Color(0xFF1A1A1A)        // Text on surfaces
val OnSurfaceVariant = Color(0xFF666666) // Secondary text on surfaces

// Button Colors
val ButtonPrimary = Color(0xFFE6C2D1)     // Primary button background
val ButtonSecondary = Color(0xFFF5E8ED)   // Secondary button background
val ButtonTertiary = Color(0x00000000)    // Transparent for text buttons
val OnButtonPrimary = Color(0xFF3D3D3D)   // Text on primary buttons
val OnButtonSecondary = Color(0xFF2D2D2D) // Text on secondary buttons

// Container Colors
val Container = Color(0xFFF9EFF3)         // General container background
val ContainerHigh = Color(0xFFF2E5EB)     // Elevated container background
val OnContainer = Color(0xFF1C1C1C)       // Text on containers
val OnContainerVariant = Color(0xFF666666)// Secondary text on containers

// TopBar/AppBar Colors
val TopBarBackground = MainColor          // Using main color for consistency
val OnTopBar = Color(0xFF3D3D3D)         // Text and icons on top bar

// Dialog Colors
val DialogBackground = Color(0xFFFFFAFC)  // Dialog background
val DialogSurface = Color(0xFFF9EFF3)     // Dialog content surface
val OnDialog = Color(0xFF1C1C1C)         // Text on dialogs

// Border and Outline Colors
val OutlinePrimary = Color(0xFFD4B0C0)    // Primary outline color
val OutlineSecondary = Color(0xFFE8DCE1)  // Secondary outline color
val OutlineVariant = Color(0xFFF0E6EB)    // Light outline for subtle borders
val Divider = Color(0xFFEDE0E5)          // Divider lines

// Icon Colors
val IconPrimary = Color(0xFF5D5D5D)      // Primary icon color
val IconSecondary = Color(0xFF8D8D8D)    // Secondary icon color
val IconOnPrimary = Color(0xFF3D3D3D)    // Icons on primary colored backgrounds
val IconDisabled = Color(0xFFBDBDBD)     // Disabled icon color

// Card Colors
val CardBackground = Color(0xFFFFFAFC)   // Card background
val CardElevated = Color(0xFFF9EFF3)     // Elevated card background
val OnCard = Color(0xFF1C1C1C)          // Text on cards

// State Colors (unchanged as they're independent)
val Success = Color(0xFF81C784)          // Success state
val Warning = Color(0xFFFFB74D)          // Warning state
val Error = Color(0xFFE57373)            // Error state
val Info = Color(0xFF64B5F6)             // Info state

// State Colors - On Colors (unchanged)
val OnSuccess = Color(0xFF1B5E20)        // Text on success background
val OnWarning = Color(0xFF663C00)        // Text on warning background
val OnError = Color(0xFFB71C1C)          // Text on error background
val OnInfo = Color(0xFF0D47A1)           // Text on info background

// Input Field Colors
val InputBackground = Color(0xFFFFFAFC)  // Input field background
val InputBorder = Color(0xFFE8DCE1)      // Input field border
val InputBorderFocused = Color(0xFFD4B0C0) // Focused input border
val InputText = Color(0xFF1C1C1C)        // Input text color
val InputPlaceholder = Color(0xFF999999) // Placeholder text color

// Floating Action Button
val FabBackground = Primary              // FAB background
val OnFab = OnPrimary                    // FAB icon color

// Selection Colors
val Selected = Color(0xFFE6C2D1)         // Selected item background
val OnSelected = Color(0xFF3D3D3D)       // Text on selected items

// Disabled Colors (unchanged)
val Disabled = Color(0xFFE0E0E0)         // Disabled background
val OnDisabled = Color(0xFF9E9E9E)       // Text on disabled elements